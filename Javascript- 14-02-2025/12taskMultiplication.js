let firstNumber = 5;  
let secondNumber = 7; 

console.log(`Multiplication Table of ${firstNumber} till ${secondNumber}:`);
for (let i = 1; i <= secondNumber; i++) {
    console.log(`${firstNumber} * ${i} = ${firstNumber * i}`);
}
